import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Rollmat Labs | Materials Solutions',
  description: 'Rollmat Labs provides high-performance composite materials for various industries, widely used in medical rehabilitation, prosthetics and orthotics, sports, footwear, and apparel.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
